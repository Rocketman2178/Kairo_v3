# Kairo QA Report — 2026-03-18

## Features Tested

- **Stripe `create-payment-intent` Edge Function (Stage 3.1)**: Full PaymentIntent creation, registration token validation, plan-based amount calculation, expiry checking, demo fallback
- **Biometric Authentication — `useBiometricAuth` + UI Components (Stage 3.1.1)**: WebAuthn hook, `BiometricAuthPrompt`, `BiometricSetupPrompt`, `BiometricSuccess`, PaymentForm and RegistrationConfirmation integration
- **Cart Recovery Edge Function — `trigger-cart-recovery` (Stage 3.4)**: Sweep and single-cart modes, timing windows, n8n integration, `recovery_attempts` counter increment
- **`useCartAbandonment` hook update**: localStorage sync on abandonment, automatic edge function call when email is known

---

## Build Status

| Stage | Result | Notes |
|---|---|---|
| Initial build | PASS | 1613 modules, no errors. Pre-existing warnings only: mixed static/dynamic import for supabase.ts, bundle >500kB |
| TypeScript check (`tsc --noEmit`) | PASS | Zero type errors |
| Final build (post-fix) | PASS | Identical to initial — fixes were to edge function (Deno) and DB schema only |

---

## n8n Workflow Tests

**Status: SKIPPED — n8n MCP tools (`mcp__n8n-hostinger__`) not available in this session.**

The n8n tools were not discoverable as deferred tools. This likely indicates the n8n MCP server was not active during this QA session. The Kai conversation workflow (ID: `K45jpp5o2D1cqjLu`) was not tested via direct API. Recommend manual verification or ensuring the n8n MCP is connected for the next run.

---

## Edge Function Tests

### `create-payment-intent` (v2 — ACTIVE)

| Test Scenario | Result | Notes |
|---|---|---|
| Code review: amount from DB only | PASS | `amount_cents` always read from `registrations` table — never client-supplied |
| Code review: token validation | PASS | Validates `registration_token` with `.eq("registration_token", registrationToken)` and status filter |
| Code review: expiry check | PASS | Checks `expires_at < now()` before creating intent |
| Code review: plan type amount calculation | PASS | `full` → 100%, `two_payment` → 50%, `divided` → 33%, `subscription` → 50% |
| Code review: demo fallback | PASS | Returns `{ demo: true }` when `STRIPE_SECRET_KEY` not set |
| Code review: error surface | PASS | No raw error details or stack traces returned to client |
| Code review: CORS | NOTE | Uses `"*"` origin — acceptable for current phase but should be restricted to known domains in production per CLAUDE.md |
| Code review: subscription plan hardcoding | NOTE | `subscription` calculates as `baseCents / 2` (assumes ~2 months). Should derive from actual session length when available |

### `trigger-cart-recovery` (v1 → v2 — ACTIVE, redeployed)

| Test Scenario | Result | Notes |
|---|---|---|
| Code review: webhook key auth | PASS | Validates `X-Webhook-Key` header when `CART_RECOVERY_WEBHOOK_KEY` is set |
| Code review: fail-open when key not configured | NOTE | If env var not set, any caller can invoke. Acceptable for dev; should document that key must be set in production |
| BUG FOUND: deepLinkUrl relative URL | FIXED | Was `/register?token=...` — broken in email clients. Fixed to `${KAIRO_APP_URL}/register?token=...` with `https://kairo.app` fallback |
| Code review: timing windows | PASS | Touch 1 (30min–2hr), Touch 2 (20–28hr), Touch 3 (68–76hr), max 7 days — matches NBC data spec |
| Code review: recovery_attempts counter | PASS | Atomically incremented only on successful n8n trigger |
| Code review: PII in response | PASS | Response contains only `{ success, cartId, attempt }` — no email or family data |
| Code review: sweep JSONB filter | NOTE | `.not("cart_data->email", "is", null)` — PostgREST JSONB path filtering; works but may not catch null vs missing key distinction. Monitor in production. |

---

## Database Validation

### Structural Checks

| Check | Result | Notes |
|---|---|---|
| `registrations.payment_intent_id` column exists | PASS | text, nullable — populated on Stripe PaymentIntent creation |
| `registrations.registration_token` column exists | PASS | text, nullable — used for anonymous flow auth |
| `registrations.expires_at` column exists | PASS | timestamptz, nullable |
| `abandoned_carts` table structure | PASS | Has `cart_data` (jsonb), `recovery_attempts` (int, default 0), `recovered` (bool, default false) |
| Foreign key relationships intact | PASS | No migrations touched FK relationships |

### Edge Case & Breaking Tests

| Test | Result | Notes |
|---|---|---|
| CRITICAL BUG: `abandoned_carts.family_id` NOT NULL | FIXED | Column was NOT NULL with no default. All anonymous cart abandonment DB inserts silently failed with NOT NULL violation. Applied migration to make nullable. |
| Constraint violation test (insert without family_id) | FIXED | Pre-fix: would throw NOT NULL violation. Post-fix: insert succeeds with family_id = NULL |
| RLS anon bypass on `registrations` | PASS | No anon SELECT/UPDATE/DELETE policy — service role only for anonymous flow |
| RLS anon bypass on `abandoned_carts` | PASS | Only org staff (authenticated, owner/admin) can access via RLS |
| RLS anon bypass on `families` | PASS | No anon policy — family-scoped to `auth.uid()` |
| RLS anon bypass on `children` | PASS | Scoped to family relationship via `family_id` lookup |
| Cascade behavior on `abandoned_carts` | PASS | `conversation_id` is nullable FK — deletion of conversation doesn't block cart records |
| Null email on single cart recovery | PASS | Edge function returns 400 `{ error: true, message: "No email address on file" }` |
| Cart not in timing window | PASS | Returns `{ skipped: true, reason: "Cart not in valid recovery window" }` with 200 status |
| Expired registration token | PASS | `create-payment-intent` checks expiry and returns 410 `{ error: true, message: "Registration has expired..." }` |

### Security Advisories (Pre-existing, not introduced by March 17 features)

| Issue | Level | Status |
|---|---|---|
| 6 SECURITY DEFINER views (`v_test_conversations`, `organization_settings_view`, `full_session_details_view`, `available_sessions_view`, `session_recommendations_view`, `v_conversation_history`) | ERROR | Pre-existing. Views were intentionally created with security definer for AI query access. Outstanding item. |
| `competitive_recommendations` — ALL policy always-true for authenticated | WARN | Pre-existing. Any authenticated user can write to all orgs' competitive data. Low risk in current single-org demo. |
| `regions`, `seasons` — ALL policy always-true for authenticated | WARN | Pre-existing. Management tables, low sensitivity. |
| `conversations` INSERT — WITH CHECK (true) for anon | WARN | Intentional — required for anonymous registration flow per CLAUDE.md spec. |
| `tiger_tank_feedback` INSERT — WITH CHECK (true) | WARN | Intentional — open feedback submission. |
| 19 functions missing `SET search_path = ''` | WARN | Pre-existing. Should be addressed systematically in a future migration. |

### Performance Advisories

| Issue | Notes |
|---|---|
| `abandoned_carts.conversation_id` unindexed FK | Pre-existing. Low impact given current table size (0 rows). |
| `families.preferred_location_id` unindexed FK | Pre-existing. |
| Additional unindexed FKs across various tables | Pre-existing from performance advisor. Add indexes as table sizes grow. |

---

## Frontend Component Tests

### `useBiometricAuth` Hook

| Check | Result | Notes |
|---|---|---|
| No server-side biometric data storage | PASS | Credential ID only stored in localStorage; no server call |
| `isBiometricSupported()` guard | PASS | Checks `window.PublicKeyCredential`, `navigator.credentials`, and `.create` |
| DOMException handling on register | PASS | Handles `NotAllowedError`, `NotSupportedError`, generic fallback |
| DOMException handling on authenticate | PASS | Handles `NotAllowedError`, `InvalidStateError` (stale credential auto-cleared) |
| localStorage failure graceful | PASS | All localStorage calls wrapped in try/catch — silent failure |
| `userVerification: 'required'` | PASS | Biometric/PIN required — cannot be bypassed |
| `attestation: 'none'` | PASS | Correct for this use case — no attestation needed |

**Minor note**: `InvalidStateError` during `authenticate()` is unlikely in practice (more common during `create()`). Stale credentials would more likely surface as `NotAllowedError`. The `InvalidStateError` branch is dead code but harmless — the error message still guides users to re-register.

### `BiometricAuthPrompt` Component

| Check | Result | Notes |
|---|---|---|
| Conditional render (not supported / not registered) | PASS | Returns null if `!isSupported || !isRegistered` |
| Loading state while authenticating | PASS | Spinner shown, button disabled |
| Error state display | PASS | `AlertCircle` + error message shown |
| Tap target size | PASS | Authenticate button: `min-h-[44px]` |
| Dismiss button accessible | PASS | `aria-label="Dismiss biometric prompt"` |

### `BiometricSetupPrompt` Component

| Check | Result | Notes |
|---|---|---|
| Conditional render (no email, already registered, not supported) | PASS | Returns null for all three cases |
| Loading state | PASS | Spinner shown, button disabled |
| Tap targets | PASS | Both "Enable Biometrics" and "Maybe later" buttons: `min-h-[44px]` |

### `useCartAbandonment` Hook

| Check | Result | Notes |
|---|---|---|
| localStorage sync on cart change | PASS | `saveCartRecovery()` called on every key field change |
| Recovery cleared on completion | PASS | `clearCartRecovery()` called when `isComplete = true` |
| DB insert blocked without `registrationToken` | PASS | Early return guards both effects |
| Event listeners cleaned up | PASS | `removeEventListener` in effect cleanup |
| Edge function fire-and-forget | PASS | `.catch(() => {})` prevents unload blocking |
| DB insert `family_id` bug | FIXED | Was silently failing with NOT NULL violation — resolved by migration |

---

## Security Vulnerabilities Tested

| Attack Vector | Protected? | Details |
|---|---|---|
| SQL Injection (create-payment-intent) | YES | Uses Supabase SDK parameterized queries — no string concatenation |
| SQL Injection (trigger-cart-recovery) | YES | All DB queries use SDK filter methods |
| XSS (biometric component) | YES | No `dangerouslySetInnerHTML`; error messages are plain strings |
| Auth bypass (create-payment-intent) | YES | Amount read from DB, token validated against DB before any Stripe call |
| Auth bypass (trigger-cart-recovery) | YES | Webhook key validated when configured; note fail-open if key not set |
| Client-supplied amount | YES | `create-payment-intent` ignores any client-supplied amount — reads only from `registrations.amount_cents` |
| Child PII in URLs | YES | No child data exposed in URL parameters or error responses |
| Biometric data server exposure | YES | No biometric data ever leaves the device — credential ID only |
| Stripe key exposure | YES | `STRIPE_SECRET_KEY` only in Deno.env, never in client code or VITE_ vars |
| Service role key exposure | YES | `SUPABASE_SERVICE_ROLE_KEY` only in Edge Functions |

---

## Issues Found & Resolved

| Issue | Severity | Resolution |
|---|---|---|
| `abandoned_carts.family_id` NOT NULL constraint broke all anonymous cart abandonment DB saves | **Critical** | Applied DB migration `20260318000001_make_abandoned_carts_family_id_nullable.sql` to make column nullable. Cart recovery system now functional. |
| `deepLinkUrl` in `trigger-cart-recovery` used relative URL (`/register?token=...`) | **High** | Fixed to use `${KAIRO_APP_URL}/register?token=...` with `https://kairo.app` fallback. Edge function redeployed as v2. |

---

## Outstanding Items

1. **n8n Workflow Tests**: Skipped due to n8n MCP unavailability. Recommend running standard + adversarial scenarios against `K45jpp5o2D1cqjLu` in the next session with the MCP connected.
2. **`KAIRO_APP_URL` env var**: Should be explicitly set in Supabase project secrets for the `trigger-cart-recovery` function to ensure the correct production domain is used (rather than relying on the `https://kairo.app` fallback).
3. **`CART_RECOVERY_WEBHOOK_KEY` env var**: Should be set in production secrets to enable the webhook authentication gate on `trigger-cart-recovery`.
4. **SECURITY DEFINER views**: 6 pre-existing views flagged by Supabase security advisor. Evaluate whether each truly needs `SECURITY DEFINER` vs can use invoker security with proper RLS.
5. **Functions missing `SET search_path`**: 19 pre-existing functions. Should be patched systematically in a dedicated migration.
6. **Bundle size**: 943kB (gzip: 229kB) — pre-existing, but worth addressing with code splitting before beta launch.
7. **`subscription` plan amount**: Hardcoded as `baseCents / 2` — should derive from actual session week count when available.

---

## Summary

The March 17 feature set (Stripe PaymentIntent, Biometric Auth, Cart Recovery) builds and type-checks cleanly. Two bugs were found and fixed: a **critical schema mismatch** that silently blocked all cart abandonment DB saves for anonymous users (family_id NOT NULL), and a **high-severity broken deep link URL** in recovery emails (relative vs absolute). Both are resolved, tested, and pushed. The biometric authentication implementation follows WebAuthn best practices with no server-side biometric data exposure. The `create-payment-intent` edge function correctly enforces server-side amount integrity. Primary outstanding item is n8n workflow live testing (MCP unavailable this session).
