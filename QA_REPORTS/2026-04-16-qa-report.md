# QA Report — 2026-04-16

**Task:** Automated QA (kairo-daily-test)
**Date:** 2026-04-16
**Commits Tested:** e55f7df, 5f3bc62

---

## Features Tested

| Feature | Build Plan Stage | Status |
|---|---|---|
| Skill-Level-Based Registration | 2.2.1 | ✅ Passed |
| Makeup Token on Cancellation | 3.7 | ✅ Passed |
| Transfer Billing Adjustment | 3.8 | ✅ Passed |

---

## Build Status

- **`npm run build`**: ✅ Passed (1.44s, 0 errors, 0 TS errors)
- **`npx tsc --noEmit`**: ✅ Passed (no type errors)
- Pre-existing warnings only: dynamic import chunking for `supabase.ts` (cosmetic, not blocking)

---

## n8n Workflow Tests (Workflow ID: K45jpp5o2D1cqjLu)

### Validation
- `n8n_validate_workflow`: Reports 9 errors, 33 warnings — **all pre-existing** (missing tool descriptions, outdated typeVersions, Format Response array return, Classify and Enrich primitive return). None introduced by today's features. Active and functional despite static validator complaints.

### Standard Tests (all via webhook trigger)

| Scenario | Response | Status |
|---|---|---|
| Basic soccer registration (5yo) | "Great! Let's sign up your 5-year-old for soccer. What's your child's name?" | ✅ |
| Multi-child (3 kids, ages 4/7/10) | Intent: `register_multi`, asked for names + sports | ✅ |
| Schedule conflict → weekday evenings | Correctly extracted `preferredDays:[1-5]`, `preferredTimeOfDay:evening` | ✅ |
| Waitlist flow | Intent: `add_waitlist`, asked for program + city | ✅ |

### Edge Case / Adversarial Tests

| Scenario | Result | Pass? |
|---|---|---|
| Empty message | Graceful greeting, asked for child info | ✅ |
| SQL injection (`'; DROP TABLE registrations; --`) | Treated as unrecognized input, greeted normally, no execution | ✅ |
| XSS injection (`<script>alert('xss')</script>`) | Treated as text, no script execution, graceful response | ✅ |
| Negative age (-3) | Extracted `childAge: -3` and continued — **no rejection** | ⚠️ |
| Unicode name (José O'Brien-Müller) | Correctly echoed back with accents and apostrophe intact | ✅ |
| Non-existent program (underwater basket weaving) | Mapped to `preferredProgram: "other"`, continued gracefully | ✅ |

**⚠️ Finding (pre-existing):** Kai accepts negative child ages without rejecting them. The DB skill gate and `age_range` check in `get_matching_sessions` protect downstream (no sessions match a negative age), but the conversational UX silently proceeds rather than correcting the user. Recommend adding age validation in the workflow's Classify and Enrich node.

---

## Edge Function Tests

**25 edge functions** reviewed — all `ACTIVE`.

| Function | verify_jwt | Status | Notes |
|---|---|---|---|
| `cancel-registration` | false | ✅ Active | Newly deployed today (v1) |
| `approve-transfer` | false | ✅ Active | Updated today (v2, +Stripe billing) |
| `kai-conversation` | false | ✅ Active | — |
| `classify-intent` | false | ✅ Active | 34 successful 200s in last 24h |
| All others | mixed | ✅ Active | No errors in logs |

**Edge function logs:** All `classify-intent` calls returned HTTP 200. No 5xx errors or timeouts in the 24-hour window. No errors logged for `cancel-registration` or `approve-transfer`.

---

## Database Validation

### Structural Checks

| Check | Result |
|---|---|
| `sessions.required_skill_level` column exists | ✅ |
| `organizations.skill_level_names` column exists (default `[]`) | ✅ |
| `registrations.cancelled_at` column exists | ✅ |
| `class_transfers.billing_applied_at` column exists | ✅ |
| All 3 new migrations applied | ✅ (20260416000001, 20260416000003, 20260416000002) |
| Fix migration applied | ✅ (20260416215426 fix_cancel_registration_rpc) |
| RLS enabled on `registrations` | ✅ |
| RLS enabled on `sessions` | ✅ |
| RLS enabled on `class_transfers` | ✅ |

### RLS Policy Verification
- `registrations`: 5 policies — family-scoped SELECT/INSERT/UPDATE + org staff SELECT/ALL. No anon-level write policies. Even though anon has table-level grants (Supabase default), RLS blocks all unauthenticated access. ✅
- `cancel_registration()` RPC: SECURITY DEFINER + `search_path = ''` ✅
- `issue_makeup_token()` RPC: SECURITY DEFINER + `search_path = ''` ✅

### Breaking / Adversarial Queries

| Test | Expected | Result |
|---|---|---|
| INSERT registration with NULL `session_id` | Rejected by NOT NULL | ✅ Rejected (23502) |
| INSERT class_transfer with invalid `billing_direction` | Rejected by CHECK constraint | ✅ Rejected (23514) |
| `get_matching_sessions` with negative age | Returns empty array (no matches) | ✅ Safe at DB layer |
| Pre-existing cancelled registrations | `cancelled_at = NULL` (column newly added) | ✅ Correct |

---

## Security Advisories

### Errors (pre-existing)
- `security_definer_view` on 6 views including `session_recommendations_view` and `available_sessions_view` (recreated by today's migration, inherited pattern). [Remediation](https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view)

### Warnings (pre-existing)
- `function_search_path_mutable` on ~22 older functions.
- `rls_policy_always_true` on `competitive_recommendations`, `conversations`, `regions`, `seasons`, `session_interest`, `tiger_tank_feedback` — intentional public-insert patterns.
- `auth_leaked_password_protection` disabled.

### Fix Applied This Run
- **NEW**: `get_matching_sessions` (8-param overload with `p_child_skill_level`) was missing `SET search_path = ''`. Fixed via migration `20260416230001_fix_get_matching_sessions_search_path`. Confirmed: `proconfig = ["search_path=\"\""]` ✅

---

## Performance Advisories

Performance advisor response exceeded token limit. No new tables or indexes introduced by today's migrations (only column additions and function updates); no new performance regressions expected.

---

## Refinements Made

### Bug Fix: `get_matching_sessions` missing `SET search_path = ''`
- **File:** `supabase/migrations/20260416230001_fix_get_matching_sessions_search_path.sql`
- **Issue:** The new 8-param overload of `get_matching_sessions` was SECURITY DEFINER but missing `SET search_path = ''`, violating CLAUDE.md standards and flagged by Supabase security advisor.
- **Fix:** Migration recreates the function with `SET search_path = ''`. Applied to DB and confirmed via `pg_proc` query.

---

## Issues Found & Resolved

| Issue | Severity | Resolved? |
|---|---|---|
| `get_matching_sessions` (new overload) missing `SET search_path = ''` | Medium | ✅ Fixed via migration |
| Kai accepts negative child ages without rejection | Low | ⚠️ Logged — pre-existing n8n behavior |
| n8n validator reports 9 errors / 33 warnings | Low | ⚠️ Pre-existing — workflow functions correctly at runtime |

---

## Vulnerabilities Tested

| Vector | Test | Result |
|---|---|---|
| SQL injection via Kai chat | `'; DROP TABLE registrations; --` | ✅ Safe |
| XSS via Kai chat | `<script>alert('xss')</script>` | ✅ Safe |
| Authorization bypass on cancel-registration | Wrong `familyId` | ✅ Family ownership verified before RPC |
| NULL injection in registrations | NULL `session_id` | ✅ Blocked by NOT NULL |
| Invalid enum in class_transfers | `billing_direction = 'invalid_direction'` | ✅ Blocked by CHECK constraint |
| Anon access to registrations | Table-level grants exist but RLS active | ✅ RLS blocks all anon access |

---

## Requires Human Testing

- **Cancel Class UI**: Two-step confirmation dialog, loading spinner, success message — requires browser testing
- **Skill level badge**: Violet GraduationCap on SessionCard/Sessions page — requires browser with seeded skill-level session data
- **Transfer billing badge**: "Billed ✓" / "(billing pending)" in Reports → Transfers tab — requires real transfer data
- **Stripe billing integration**: Off-session charges and refunds on `approve-transfer` — requires Stripe test keys
- **Makeup token flow**: End-to-end cancel → token issued → token redeemed — requires authenticated family session

---

## Outstanding Items

1. **Kai age validation**: Negative and out-of-range ages should produce a helpful correction rather than silent continuation. Recommend validation in Classify and Enrich n8n node.
2. **`get_matching_sessions` (5-param old overload)**: Still missing `SET search_path = ''`. Pre-existing, should be batched into a future cleanup migration.
3. **n8n validator errors**: Tool descriptions missing on 6 Code Tool nodes, outdated node versions. Pre-existing. Degrades LLM tool-selection reliability.
4. **No sessions with `required_skill_level` in test data**: Skill gate logic validated via SQL/code review but cannot be end-to-end tested without seeded skill-level sessions.

---

## Summary

**Build:** ✅ Clean
**n8n Workflow:** ✅ All standard + adversarial tests passed (1 pre-existing UX gap on age validation)
**Edge Functions:** ✅ Both new functions active, no errors in logs
**Database:** ✅ All 4 new columns present, constraints enforced, RLS verified
**Security:** ✅ 1 bug found and fixed (missing search_path), injection attacks all blocked
**Pushed:** ✅ Fix migration committed and pushed to origin/main

---

## Step 1: Detect New Work

**Result:** No unpushed commits detected.

```
git log origin/main..HEAD --oneline
(empty)
```

Branch `main` is fully in sync with `origin/main`. The working tree is clean.

Most recent commits on record:
- `7565f45` Fix 5 QA failures from 2026-04-15 simulation report
- `99bfd70` test: user simulation report 2026-04-15
- `cd13679` test: user simulation QA report 2026-04-14
- `d4ab720` feat: admin token dashboard, expiration warnings & waitlist-impact transfers
- `935acca` test: QA report 2026-04-14

---

## Summary

No new work was committed by the dev task (`kairo-daily-dev`) for this cycle. All QA steps (build verification, n8n workflow tests, edge function tests, database validation, frontend review) were skipped as there is nothing new to evaluate.

**Next scheduled run:** 2026-04-17 (or whenever dev task commits new work)
